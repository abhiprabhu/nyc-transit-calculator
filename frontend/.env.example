# In local dev, leave this unset — Vite proxies /api to localhost:3001
# (see vite.config.js). In production (Vercel), set this to the deployed
# backend's URL, e.g. https://nyc-transit-api.onrender.com
# VITE_API_URL=https://nyc-transit-api.onrender.com
